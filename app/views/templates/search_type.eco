<% if @types.length: %>
<select>
  <% for type in @types: %>
    <option value="<%- type.name %>" <% if @selectedType is type.name: %>selected<% end %>>
      <button><%- type.name %></button>
    </option>
  <% end %>
</select>
<% end %>